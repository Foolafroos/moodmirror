# MoodMirror Phase 0 — aucune dépendance externe.
# Python >= 3.9, stdlib uniquement (sqlite3, urllib, html.parser, json).
# Zéro provider, zéro LM Studio : l'analyse de contenu est 100 % locale
# (lexique sentiment FR/EN hors-ligne + extraction de texte locale).
